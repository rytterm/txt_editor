#include <stdio.h>
#include "macro.h"
#include "state.h"
#include "input.h"
#include <stdlib.h>
#include "file.h"

int main(int argc, char** argv) {
    int state = NO_STATE;

    //create(state,"test.txt");
    //fdelete(state,"test.txt");
    
    
    /*while(1) {

        state = INSERT_STATE;
        char* txt = get(state);
        printf("%s\n",txt);
        free(txt);

    }*/
    return 0;
}